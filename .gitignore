# Secrets / local env
.env
*.env
!.env.example

# TLS material for local https. The private key in here is a real private key;
# committing it would publish it. Regenerate with:
#   python backend/scripts/generate_dev_cert.py
backend/certs/
*.pem
*.key
*.pfx
*.p12

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Node / Expo
node_modules/
.expo/
dist/
web-build/
*.log
*.err
npm-debug.*
yarn-debug.*
yarn-error.*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Claude Code local state
.claude/scheduled_tasks.lock

# MCP server config. Points at an absolute path to a per-machine virtualenv,
# so it is local setup rather than something a teammate could use.
.mcp.json

# Never commit real user/health data
**/real-data/
**/*real-patient*

# Local index cache written by the token-savior MCP server.
.token-savior-cache.json

# Cached MedlinePlus responses for the retrieval-accuracy harness. Several
# hundred KB of someone else's data, regenerated by re-running the script:
#   python backend/scripts/topic_retrieval_eval/measure.py
backend/scripts/topic_retrieval_eval/upstream_cache.json

# Tesseract's English model, downloaded at runtime by tesseract.js. 5 MB of
# generated data, not source - the web scanner fetches it from a CDN.
mobile/eng.traineddata

# Agent cycle transcripts (run_cycle.sh)
.agent-cycles/

# LICENSED telephone-triage protocol content, loaded by
# backend/app/core/protocol_content.py. It is somebody else's copyrighted
# clinical material and this project has no right to redistribute it, so it
# lives outside the tree and PROTOCOL_CONTENT_DIR points at it.
#
# The ignore is a backstop, not the rule. tests/test_protocol_content.py
# asserts no protocol-shaped file exists anywhere in the repository.
backend/protocol_content/
**/*.protocols.json

# The mutation checker's scratch copy. It lives under mobile/ because jest
# resolves node_modules by walking up from rootDir, and it is removed in a
# finally — this is here for the run that gets interrupted anyway.
mobile/.mutation-scratch/

# Local SQLite databases. A dev run creates one, and `git add -A` swept one
# into a commit on 2026-09-14 — synthetic, but it carried an account row and a
# password hash, and a database file has no business in this repository.
*.db
*.sqlite
*.sqlite3

# Local agent tooling (not part of the app)
.agents/
.claude/skills/
.claude/settings.json
.claude/CLAUDE.md
.claude/worktrees/
skills-lock.json
graphify-out/
