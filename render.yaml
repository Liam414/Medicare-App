# Render blueprint — stands the whole app up on the public internet.
#
# WHAT THIS CREATES
#
#   medhelp-web-as615   the app itself, a static site (the link you share)
#   medhelp-api-as615   the FastAPI backend
#   medhelp-db          Postgres
#
# HOW TO USE IT: see docs/deployment.md. Short version — push this file to
# GitHub, then on Render: New > Blueprint > pick this repository > Apply.
#
# ⛔ READ THIS BEFORE DEPLOYING. CLAUDE.md and README used to say, in bold,
# not to expose this backend to the open internet, and the release blockers
# that warning rests on are all still open: no clinician has reviewed the
# triage instrument, no lawyer has ruled on medical-device status, nothing is
# encrypted at rest, and there is no BAA with any vendor. Publishing this was
# an explicit decision by the repository owner on 2026-09-02, recorded in
# CLAUDE.md under "Public deployment". It is a demo of software, not a service
# for real patients, and the app says so on every screen.
#
# WHAT PUBLISHING CHANGES, AND WHAT IT DOES NOT
#
#   * TLS is real here, not the self-signed certificate the LAN setup uses.
#     Render terminates a CA-issued certificate, so traffic is both encrypted
#     and authenticated — which also makes the browser's geolocation API work
#     for the first time.
#   * Every route still requires a bearer token and filters on the caller,
#     sign-in is still rate limited, and CORS is an explicit allowlist.
#   * Still missing: encryption at rest, token revocation, an audit log of
#     reads, and a least-privilege database role. Those are unchanged by
#     deploying — see "Open data-handling findings" in CLAUDE.md.

databases:
  - name: medhelp-db
    databaseName: medhelp
    user: medhelp
    plan: free
    # Same region as the API, explicitly. The API reaches the database over
    # Render's internal network, which only exists within one region.
    region: oregon

services:
  # ---------------------------------------------------------------- backend
  - type: web
    name: medhelp-api-as615
    runtime: python
    plan: free
    region: oregon
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    # Alembic is still not wired up (CLAUDE.md, "Known Gaps"), so nothing
    # creates a table for a new model. `create_missing_tables.py` calls
    # `create_all`, which creates what is missing and never alters what
    # exists, so it is safe to run on every boot. Replace this with a real
    # migration step before anything but a demo runs here.
    #
    # ⛔ `create_all` NEVER ALTERS AN EXISTING TABLE, so a column added to a
    # model that already has a table needs its own script — and leaving those
    # to be run by hand does not work. The refill columns went onto
    # `medications`, which has existed for months; the deploy came up clean,
    # reported healthy, and then returned 500s from every read of
    # `/medications`, because the health check does not touch the table. A
    # migration nobody runs is a migration that does not exist.
    #
    # Both scripts are `ADD COLUMN IF NOT EXISTS` and are idempotent, so
    # running them on every boot costs a round trip and closes that gap.
    # ⛔ Add the script here when you add a column to an existing model.
    startCommand: >-
      python scripts/create_missing_tables.py &&
      python scripts/add_intake_audit_columns.py &&
      python scripts/add_medication_supply_columns.py &&
      python scripts/add_goal_schedule_columns.py &&
      python scripts/add_goal_detail_columns.py &&
      python scripts/add_care_profile_columns.py &&
      uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      # 3.13 has no wheel for some of the pinned versions here; 3.11 is what
      # CLAUDE.md names and what the lockfile was resolved against.
      - key: PYTHON_VERSION
        value: "3.11.9"

      # Not one of local/dev/development/test/testing, so `app/core/config.py`
      # applies its production rules: a weak signing key is a refusal to boot,
      # a "*" CORS origin is refused, /docs is not served, and
      # `app/db/session.py` appends sslmode=require to the database URL.
      - key: ENVIRONMENT
        value: production

      - key: DATABASE_URL
        fromDatabase:
          name: medhelp-db
          property: connectionString

      # ⛔ The whole of authentication. Render generates a random value and
      # never shows it in the blueprint, which is the only way this stays out
      # of source control. If the API fails to boot complaining the key is too
      # short, set one by hand in the dashboard:
      #     python -c "import secrets; print(secrets.token_hex(32))"
      - key: JWT_SECRET_KEY
        generateValue: true

      # The exact origin the app is served from. This is a literal rather than
      # a `fromService` reference to the static site, because the site already
      # references this service and Render will not resolve a cycle.
      #
      # ⛔ If Render gave the static site a different URL — it appends a suffix
      # when a name is already taken globally — change this to match, or every
      # request from the browser fails CORS with no useful error on screen.
      - key: CORS_ALLOW_ORIGINS
        value: https://medhelp-web-as615.onrender.com

      # Loopback/LAN origins are a development convenience and are ignored
      # outside a development ENVIRONMENT anyway. Off explicitly, so nothing
      # depends on that.
      - key: CORS_ALLOW_PRIVATE_ORIGINS
        value: "false"

      - key: ENABLE_API_DOCS
        value: "false"

      # Optional, and it changes what the symptom check can do rather than
      # whether it works: the deterministic rule layer always runs and needs no
      # key, but without one the explanation is a fixed sentence rather than
      # one written to what the user typed, and the classifier can never ask a
      # clarifying question. Left unset here — Render prompts for it — because
      # a key in a blueprint is a key in source control.
      #
      # ⛔ Setting it makes Anthropic a processor of symptom descriptions, and
      # this project has a BAA with nobody. See CLAUDE.md.
      - key: ANTHROPIC_API_KEY
        sync: false

      # The free alternative: any OpenAI-compatible endpoint. Setting a base
      # URL and a model switches the model layer to the agentic deduction loop
      # (app/core/deduction.py) and costs nothing. Also left unset here, and
      # for the same reason — a key in a blueprint is a key in source control.
      #
      # ⛔ ON A DEPLOYMENT THIS CAN ONLY BE A HOSTED ENDPOINT, AND THAT MEANS
      # TRANSMITTING HEALTH DATA. The local option that raises no BAA question
      # (Ollama on localhost) cannot run on a free instance — there is not
      # enough memory, and nothing is listening on localhost here. So setting
      # these makes Groq, Google or OpenRouter a processor of symptom
      # descriptions typed into a public URL, and this project has a BAA with
      # nobody. Same decision as ANTHROPIC_API_KEY above, not a cheaper way
      # around it. See docs/free-model-setup.md.
      - key: LLM_BASE_URL
        sync: false
      - key: LLM_MODEL
        sync: false
      - key: LLM_API_KEY
        sync: false

      # Health goals may use a DIFFERENT endpoint from symptom triage. Each
      # falls back to its LLM_* counterpart when empty, so leaving all three
      # blank is exactly the behaviour of not having them.
      #
      # THIS MATTERS MORE HERE THAN LOCALLY. On a dev machine the split lets
      # goals use a hosted model while symptoms stay on Ollama. There is no
      # local option on a free instance, so the useful configuration here is
      # the other one:
      #
      #   set GOALS_LLM_* and leave LLM_* unset
      #
      # That gives goal suggestions a model while symptom triage runs on the
      # deterministic rule layer alone — which CLAUDE.md calls the product,
      # the model being an optional upgrade. No symptom description is
      # transmitted anywhere at all, which is the strongest position available
      # on a public deployment with no BAA.
      #
      # ⛔ It still transmits goal text, which is health free text about an
      # identified user, to a vendor this project has no agreement with. It
      # narrows the exposure; it does not remove it.
      - key: GOALS_LLM_BASE_URL
        sync: false
      - key: GOALS_LLM_MODEL
        sync: false
      - key: GOALS_LLM_API_KEY
        sync: false

      # The one-setting form of the three above, for Groq: the key names the
      # vendor, so the app supplies Groq's base URL and model itself. Setting
      # this alone gives goal drafting a model while symptom triage stays on
      # the deterministic rule layer — the strongest position available on a
      # public deployment with no BAA, since no symptom description is
      # transmitted anywhere at all.
      #
      # It still transmits goal text, which is health free text about an
      # identified user, to a vendor this project has no agreement with.
      - key: GROQ_API_KEY
        sync: false

      # ⛔ Both off, and neither should be switched on here. The first attaches
      # MedlinePlus reading material that a compliance review rejected; the
      # second writes symptom descriptions to the application log.
      - key: MEDLINEPLUS_TOPICS_ENABLED
        value: "false"
      - key: TRIAGE_LOG_CLASSIFICATIONS
        value: "false"

      # The in-memory limiter is per process (CLAUDE.md, open finding 6). One
      # free instance means one process, so these are the real numbers.
      - key: AUTH_RATE_LIMIT_ATTEMPTS
        value: "10"
      - key: AUTH_RATE_LIMIT_WINDOW_SECONDS
        value: "300"

  # --------------------------------------------------------------- the app
  - type: web
    name: medhelp-web-as615
    runtime: static
    rootDir: mobile
    # `expo export` writes the web build to mobile/dist, which is gitignored —
    # it is built here on every deploy rather than committed.
    #
    # EXPO_PUBLIC_* is inlined into the bundle by babel at build time, so the
    # API address is baked in here and cannot be changed without rebuilding.
    # That is why it is composed from the API service's real hostname rather
    # than guessed: `baseUrl.ts` otherwise looks for the API on port 8000 of
    # whatever host served the page, which is right on a LAN and wrong here.
    # ⛔ `property: host` returns the service NAME, not a resolvable hostname.
    # This built EXPO_PUBLIC_API_BASE_URL as "https://medhelp-api-as615",
    # which resolves nowhere, so the deployed app loaded and then failed every
    # request with "Can't reach the MedHelp server" — including sign-in. The
    # API was healthy the whole time; only the address baked into the bundle
    # was wrong.
    #
    # `${VAR%.onrender.com}` strips the suffix if it is already there before
    # appending it, so this is correct whichever form Render returns.
    buildCommand: >-
      npm ci &&
      EXPO_PUBLIC_API_BASE_URL="https://${MEDHELP_API_HOST%.onrender.com}.onrender.com"
      npx expo export --platform web
    staticPublishPath: dist
    envVars:
      - key: NODE_VERSION
        value: "20.18.0"
      - key: MEDHELP_API_HOST
        fromService:
          type: web
          name: medhelp-api-as615
          property: host
    routes:
      # Single-page app: a deep link or a refresh on any path has to be served
      # index.html rather than a 404, or the router never gets to run.
      - type: rewrite
        source: /*
        destination: /index.html
    headers:
      # The API sets its own, stricter set in `app/main.py`. These cover the
      # HTML and the bundle, which that middleware never sees.
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
      - path: /*
        name: Referrer-Policy
        value: no-referrer
      - path: /*
        name: X-Frame-Options
        value: DENY
      # ⛔ THE WEB APP HAS NO CONTENT-SECURITY-POLICY, AND CLAUDE.md RELIES ON
      # ONE. `docs/security.md` argues for keeping the session token in
      # `sessionStorage` rather than `localStorage` and says plainly: "Neither
      # store is protected from script running on the page — the defence
      # against that is the CSP, not the choice of store." That defence was
      # never deployed. The API's `default-src 'none'` governs API responses,
      # not the HTML page where script actually runs.
      #
      # ⛔ REPORT-ONLY ON PURPOSE, AND THIS IS NOT THE FINISHED JOB.
      # `Content-Security-Policy-Report-Only` blocks nothing. It cannot break
      # the app, which matters because an enforcing policy that is wrong takes
      # the whole site down for everyone, and the exact origins are not
      # knowable from this repository: `tesseract.js` fetches its WASM core and
      # English training data from a CDN chosen inside the library at runtime,
      # so the host depends on the pinned version rather than on any line we
      # wrote. Guessing it wrong silently breaks label scanning on the web.
      #
      # HOW TO FINISH THIS:
      #   1. Deploy with this Report-Only header.
      #   2. Open the app, sign in, and use the label scanner — the scanner is
      #      the path that reaches a third-party origin, so it must be
      #      exercised or the report will look clean and be incomplete.
      #   3. Read the browser console. Every violation names the directive and
      #      the blocked origin.
      #   4. Add those origins here, then rename this header to
      #      `Content-Security-Policy` to start enforcing.
      #
      # `connect-src` names the API by literal hostname for the same reason
      # `CORS_ALLOW_ORIGINS` does: the web service already references the API,
      # and Render will not resolve a cycle. A rename of the API service needs
      # this line changed by hand.
      - path: /*
        name: Content-Security-Policy-Report-Only
        value: >-
          default-src 'self';
          base-uri 'none';
          object-src 'none';
          frame-ancestors 'none';
          form-action 'none';
          script-src 'self' 'unsafe-eval';
          style-src 'self' 'unsafe-inline';
          img-src 'self' data: blob:;
          font-src 'self' data:;
          worker-src 'self' blob:;
          connect-src 'self' https://medhelp-api-as615.onrender.com
